# PitchMi — TASKS

Backlog and follow-ups. Active v1 build is T-1150 (see `docs/cc-goal-pitchmi-v1.md`).

| ID | Task | Status | Notes |
|---|---|---|---|
| T-10024 | Second visual pass — custom player, desktop layout, one accent, composer fills the viewport | done | Spec `docs/cc-goal-visual-pass-2.md`; direction appended to `docs/design-direction.md` as `## T-10024 — second pass`. **Refero MCP was connected and was used again** — 11 new cited references, each read as an image or as metadata and each carrying one concrete borrowed decision (Flask.do, Anam ×2 — once as a *don't*, TwelveLabs, Metaview, Cursor, Wabi, Default, shadcn/ui, OpenSea, Linear's product site). **(1) `VideoPlayer`** — new `components/ui/VideoPlayer.tsx` replaces the browser's `<video controls>` on `/results`: play/pause, `m:ss` in tabular figures, an accent scrubber and mute, as a row *beneath* the frame (Flask.do), no fullscreen and no rate control, click-anywhere and Space to toggle, `playsInline` kept and asserted. Handles `duration` `NaN` (the dev fixture blob never decodes — which is what makes the screenshots reviewable), `Infinity` (the MediaRecorder/WebM quirk) and scrub-before-metadata, all as elapsed-only / no-op rather than `NaN:NaN` or a throw. **(2) Desktop** — two content widths declared once as `.shell` (32→40rem) and `.shell-wide` (32→60rem); `/results` becomes a real `lg` grid (video | score, then coach feedback | actions) with DOM order = visual order in both layouts, so the mobile stack is unchanged. **(3) One accent** — indigo `#4f46e5` / `#818cf8`, the hue furthest from all three score bands (≈244° vs 142/32/0), AA in every context it is used in with the ratios written down (worst case 4.79:1 against a 3:1 threshold), registered in `utils.ts`, spent on exactly three things: the wordmark, the one filled action + focus rings, and progress/position. **(4) Composer** — the writing surface grows to fill; ≥80% of viewport height at 390×844 and 1440×900, no filler added, still top-aligned on a phone. **New permanent guard:** a token-parity Vitest parses `@theme` and fails if any token is missing from `cn()`'s merge list. **Deviation:** `results.test.tsx`'s filled-button assertion rewritten `bg-primary` → `bg-accent` (intent unchanged, not deleted). Zero files under `server/`; `Prompter.tsx`, `useRecorder.ts`, the karaoke clock, every `lib/` constant and `/karaoke` untouched. Client 113 vitest green + `next build` exit 0 + `npx playwright test` 45/45 including `prompter-rtl.spec.ts` 2/2. 30 PNGs in `docs/ui/` at 390×844 / 1024×768 / 1440×900 in both schemes. |
| T-10021 | New slogan + delete the use-case picker (UI **and** prompt) | done | Spec `docs/cc-goal-drop-use-case.md`. Shipped as specced, no deviations. Home screen is now header → script card → Record it; `UseCase`/`USE_CASES`/`useCase`/`customText` and the whole picker `<Card>` are gone, `createProject()` takes no arguments (`data?: { title? }`, posts `{}`) and the Record button gates on nothing but `loading` and an empty `text`. `Project.use_case`/`use_case_custom` stay on the TS interface — the server still returns those columns. Prompt: `USE_CASE_GUIDANCE` + the `{{USE_CASE_GUIDANCE}}` placeholder deleted, `buildPrompt({ language })` now does a single substitution, and `routes/evaluate.js` stops passing the two fields. Folded-in stale-copy fix: `eval.md` said ≤ 60 seconds / ≤60s in two places, wrong since the T-1172 30s cap — both now 30. Guard test added: the built prompt must contain no `{{` at all, so an orphaned token can never reach the model. **No migration and no server API change** — `projects.use_case`/`use_case_custom` keep their `'pitch'` default and `POST /api/projects` still validates the optional field; verified directly that no body → 201 `'pitch'`, `{use_case:'sales'}` → 201, `{use_case:'nonsense'}` → 400 `bad_use_case`, and `api.killswitch.test.js` passed untouched. `db.js`, `score.js`, `scriptPath.js`, `subtitles.js`, `Prompter.tsx`, `useRecorder.ts` and every other screen untouched; no restyling (that is T-10022). Server 192 Jest + standard green; client 95 vitest + `next build` exit 0 + layout proof 2/2; home screenshotted at 390×844 to confirm no empty card shell. **Why (Eli):** the picker was a five-way choice a first-time user had to answer before typing a word, and its only downstream effect was one sentence of tone guidance in the eval prompt — one line of nuance at the cost of the first interaction in the product. |
| T-10022 | Release UI pass, researched against real shipped products | done | Spec `docs/cc-goal-release-ui.md`; direction written up in `docs/design-direction.md`. **Refero MCP was connected and was used** — no fallback list needed; 10 cited references (Teal, Grammarly, Duolingo, Airtable, Hers, Meiro, WhatsApp, Product Hunt, Linear, Cron/Metaview/Pipe), each with one concrete borrowed decision. Semantic tokens in `globals.css` (single raw-hex palette block → `canvas/surface/raised/line/track/fg*/primary*/good|warn|bad ×3/danger/media`), six type steps, two radii, **no shadows** in the product surface, no brand accent — the only hues are the three score bands. Dark mode follows the OS, no toggle; **grep proves it** — zero raw palette steps left in any in-scope screen or UI component. `/karaoke`, the `/videos` player and `AdSlot` carry a new `.scheme-dark` class that pins the dark values in BOTH schemes (verified by computed style, since they can't be screenshotted). `next/font` Inter (latin) + Heebo (hebrew) in one stack — Inter has no Hebrew at all. Review artifact: 16 committed PNGs in `docs/ui/` (`/`, `/results`, `/wait`, `/wait` error × 390×844 + 1440×900 × light + dark) from new `client/tests-layout/screens.spec.ts` + dev fixtures at `/dev/ui/[screen]`. **Deviations:** `/wait`'s markup extracted to `components/WaitView.tsx` (logic untouched) so the fixture can shoot the real screen without a live eval; `lib/utils.ts` `cn()` now uses `extendTailwindMerge` (tailwind-merge guessed every custom `text-*` token to be a font size and silently dropped button label colours); two class assertions rewritten deliberately, not deleted — `results.test.tsx` `bg-green-600` → "the one primary/filled button" (green now means score ≥ 80), `home.test.tsx` `/amber/` → `/text-warn-fg/`. Zero files under `server/`; `Prompter.tsx`, every hook, every `lib/` constant and the store untouched. Client 95 vitest green + `next build` exit 0 + `npx playwright test` 18/18 including the RTL proof 2/2. |
| T-10023 | Decide whether the ad slot ships at launch | backlog — Eli decision | Release goes out **without ads** (ads after ~100K impressions/month — video SSPs will not onboard below roughly that). But `AdSlot` still renders on `/wait` whenever `GET /api/ad` returns a config, so a launch build can show a placeholder ad to real users. Needs an explicit gate (an `ADS_ENABLED` server flag is the cheap version) rather than relying on what the stub happens to return. Deliberately kept out of T-10022, which is presentational only. |
| T-1150 | v1 core loop — greenfield build | done | Server + client per v1 spec. All §13 acceptance checks pass (70 automated tests). Pending: real-key smoke + manual QA. |
| T-10010 | Public-release hardening (10 audit findings) | done | Spec `docs/cc-goal-release-hardening.md`. (1) `ADMIN_COOKIE_SECRET` replaces the hardcoded cookie key + `SUPABASE_JWT_SECRET` reuse; server refuses to boot without it or `ADMIN_PASSWORD`. (2) `express-rate-limit` 7.5.1 + `trust proxy 1`: login 10/15min, transcribe/evaluate 30/h, save 20/h, `rate_limited` event on trip. (3) ffmpeg `-protocol_whitelist file` + explicit `-f`, container decided by magic bytes (see STATUS deviation — the specced mime allowlist would have 415'd every Chrome take). (4) `lib/jobLimiter.js` semaphore, `MEDIA_CONCURRENCY=2`/`MEDIA_QUEUE_MAX=8`, over-queue → 503 `busy`. (5) `-nostdin` + closed stdin + 60s SIGKILL timeout. (6) Scribe body read moved inside the abort timer. (7) `timingSafeEqual` login + `admin_login` event. (8) admin cookie `SameSite=None; Secure` unconditionally (cross-origin client). (9) CORS fails closed on empty `CLIENT_ORIGIN`. (10) karaoke error state + cancellable countdown + try/catch around `MediaRecorder`. Server 142 Jest + standard green; client 82 vitest + build + layout 2/2 green. Untouched per spec: surge design, kill-switch fail-open, helmet, JWT claim checks, `save.js` contentType, disk storage, DB schema, scoring/prompts. |
| T-10018 | Text-first flow — typed script, seeded path, pace learned from every take | done | Spec `docs/cc-goal-text-first-flow.md`, **partly superseded mid-build by an owner scope change** (see STATUS deviation): the specced mic-only `/read` screen + `POST /api/calibrate` were dropped in favour of seeding the path from an estimate and re-timing on every karaoke take. The alignment core is common to both and shipped as specced. **New** `server/src/lib/scriptPath.js`: `buildPathFromScript(script, spokenWords)` aligns the typed script against a take via the already-exported `align()` from `score.js` (unmodified) and gives every matched word its **exact** spoken timestamp; unmatched words are interpolated between real neighbours proportionally to `charLen`, with `t_start` forced non-decreasing (the karaoke clock breaks silently otherwise). `buildSeedPath(script)` is a ~40-line disposable estimate — `SEED_CHARS_PER_SECOND=13` (chars, not WPM, so Hebrew is not mis-paced) plus T-1168's 80/150/350ms word/clause/sentence pauses — that survives exactly one take. **No pacing engine was rebuilt** (explicit non-scope). New `POST /api/script` stores `{script, path: seed}` and returns `{path, word_count, est_duration_s}`; re-posting always re-seeds, so edited text can never rehearse against stale timings. `/api/evaluate` realigns **after** `scoreTake` (so the take is judged against the path it followed) and at `coverage >= MIN_ALIGN_COVERAGE` (0.5, an admitted guess — logged per-event for tuning, `FU-COVERAGE-THRESHOLD`) persists + returns the re-timed path. **Deleted:** `/api/transcribe` + its limiter, `/recorder`, the home upload path. **Client:** `/` is a script textarea with a `dir`-aware live estimate and an in-place amber over-length tail (flag only — no AI rewrite, no auto-trim, never blocks); `/wait` adopts a returned path; `/results` gains Edit text. `useRecorder.ts`, `upload.js`, `Prompter.tsx`, `score.js` untouched. Server 190 Jest + standard green; client 94 vitest + build + layout 2/2 green. |
| T-10019 | `MIN_ALIGN_COVERAGE = 0.5` has no data behind it | todo | From T-10018. The threshold decides whether a take is trusted to re-time the prompter, and it was picked by judgment, not measurement. Every `evaluate` event now logs `scores.coverage` + `scores.retimed`; after a week of real takes, compare coverage on takes that felt like real deliveries against ones that did not and set it honestly. Synthetic evidence so far: paths stay usable (monotonic, one word per typed word, distinct starts) well below 0.5, so the threshold is about trust, not about the algorithm falling apart. |
| T-10011 | Rate limiter + media semaphore are per-instance | todo | From T-10010. Both use in-process memory, so the per-IP ceilings and the ffmpeg concurrency bound are per container. Fine on today's single Railway instance; horizontal scaling silently multiplies every limit by the instance count. Needs a shared store (Postgres/Redis) or sticky routing before scaling out. |
| T-10012 | `videos` bucket is not reproducible from the repo | todo | From T-10010's Supabase check. The bucket exists and is correctly private with no `storage.objects` policies (service-role only), but `0001_init.sql` only *describes* it in a comment — nothing in the repo creates it or its policies, so a fresh project cannot be stood up from migrations alone. |
| T-10016 | No security response headers | todo | Deferred out of T-10010 scope by the PM. No `helmet`: no HSTS, `X-Content-Type-Options`, frame-ancestors or referrer policy on the API. |
| T-10013 | Error handler echoes `err.message` to clients | todo | Deferred out of T-10010 scope. `errorHandler` returns `{error:'server_error', message: err.message}`, which can leak internal detail (upstream URLs, file paths) to any caller. |
| T-10014 | JWT verification checks signature only | todo | Deferred out of T-10010 scope. `middleware/auth.js` verifies the signature but not `aud`/`iss`/`role`, so any token signed by the same project keys is accepted. |
| T-10015 | `/api/save` trusts the client's contentType | todo | Deferred out of T-10010 scope. `save.js` passes `req.file.mimetype` straight into Storage upload metadata; a stored object can therefore be labelled anything. Now that `sniffContainer` exists (T-10010), the honest container is one call away. |
| T-1172 | 30-second hard cap + real cost accounting | done | Absorbs T-1171 (superseded). Product call (Eli): "If you can't say it in 30 seconds, you can't say it" — hard auto-stop at 30.0s, no grace, no trimming. (A) Cap: `MAX_TAKE_S` shared client constant replacing the bare `60` in `recorder/page.tsx:13`, `useRecorder.ts:41`, `page.tsx:152` copy, `karaoke/page.tsx:29` fallback (karaoke's `gracePeriod=3` stays — it's bounded by take 1). Ring countdown rescaled: amber <=10s, red <=5s, pulse <=3s. `useRecorder` gains `stopReason: user|limit`; a timer-stopped take shows a "Time's up" interstitial before transcribing, because take 1 defines `projects.path` and a severed script poisons every rehearsal scored against it. (B) **Server enforcement, which does not exist today** — the 60s limit is browser-only, the sole server bound is `MAX_UPLOAD_MB`, and `handleUpload` has no duration check at all. New `MAX_TAKE_S`/`TAKE_TOLERANCE_S` config; duration read free from `extractAudio`'s own ffmpeg `time=` stderr (Chrome MediaRecorder webm has no header duration, so `ffmpeg -i` Duration: parsing does NOT work); >33s -> 413 `take_too_long` before Scribe is called; unparseable -> fail open. (C) Cost: `SCRIBE_USD_PER_MIN` 0.007 -> 0.004 (~75% overstated), billed off real media duration not last-word-end, and OpenRouter `usage:{include:true}` captured so the eval spend (~45% of a pass, currently invisible) lands in `scores.cost` + `cost_usd`; admin gains stt/eval split. NOTE: the earlier "cost_usd is double-counted across transcribe+evaluate" claim was WRONG — those are two separate Scribe calls on two different videos, so the sum is correct; no migration. Non-scope: no `media_resolution` (no saving exists on Gemini 3.x), no Groq STT swap (needs Hebrew A/B). Spec `docs/cc-goal-30s-cap-and-cost-truth.md`. |
| T-1171 | Cost accounting truth-up (PITCHMI-COST-ACCOUNTING) | superseded by T-1172 | Folded into T-1172 §4 and shipped there — do not run separately. Both halves landed in the same four files (`config.js`, `lib/audio.js`, `routes/transcribe.js`, `routes/evaluate.js`) and the duration probe added for the cap is the correct billing basis for the cost fix, so splitting them would have meant two passes and a guaranteed conflict. |
| T-1170 | Timing-offset normalization + results-screen actions | done | (A) P0 `lib/score.js`: a constant lead/lag collapsed the timing score — same clip that set the subtitles → 100/no flags, identical rhythm shifted +1.2s → **66 with every line `dragged_line`** (tester saw the mirror case: 31, all lines Rushing), because per-word offsets were measured against the absolute subtitle clock. Fix: `medianOffset` over matched words, and `relOffset = offset − medianOffset` now drives `mean_abs_offset`/the timing curve (unchanged 0.3s→3.0s shape)/drift/per-line rushed+dragged means/`long_pause`; `off_script` + `skipped_line` untouched. (B) Results: three buttons only — Try again (green primary → `/karaoke`), New video, Share; Download + Save-to-cloud removed (OS share sheet covers both, with a direct-download fallback when `canShare({files})` is false). "My saved videos" home nav hidden; `/videos`, `/api/save`, `/api/takes/:id/url` and the bucket left functional but unlinked (deliberate MVP decision, v2 re-links). Server 75 Jest + standard green; client 63 vitest + build + layout 2/2 green. Untouched: auth, DB schema, evaluate/provider pipeline, transcribe/subtitles, prompts, RTL renderer, admin. |
| T-1169 | Zero-edit MVP flow: remove editor + pacing engine, subtitles at original pace | done | Product call: editing re-paced karaoke and never felt like the user's own rhythm (repeated T-1167/68 failures), so DELETE editing. Removed `/editor` screen+route (recorder/upload → straight to `/karaoke`), `cleanVerbatim.js`+call, the whole pacing engine in `lib/path.js` (`measure*`/`layUniform`/`layAnchored`/`smoothRates`/anchors/comfort/LCS) + `POST /api/path`, client speed slider + `speed`/`editedScript` state. Kept: filler stripping (dropped filler → wider gap, neighbours keep exact timings); phrase-boundary line grouper moved verbatim to `lib/subtitles.js` (`buildSubtitles`); Prompter/scoring/evaluate untouched. `/api/transcribe` now strips fillers → builds karaoke structure from each surviving word's real timestamp → persists to `path` jsonb, returns `{script,language,path,duration_s}`; silent take → `422 no_speech`. Eval prompt additive: comments may coach pace/timing, persona/count/length/language/messenger rules unchanged. `projects.speed`/`original_words` left in place, unused (schema untouched). Server 70 Jest + standard green; client 61 vitest + build + layout 2/2 green. |
| T-1168 | Tester round 2: karaoke too fast after editing | done | Server `lib/path.js` only. Uniform fallback ran faster than real speech because `measureRate` uses the voiced-only median and `layUniform` packed words gapless. Fix: new `measureEffectiveRate` = `(last.end−first.start)/total_chars` over `original_words` (includes gaps), floored by the voiced median → `effRate`; `layUniform` lays explicit gaps (WORD_GAP 80ms / CLAUSE_GAP 150ms / SENTENCE_PAUSE 350ms) with word durations chars×effRate×COMFORT 1.1; anchored inserted/lead/trail runs pace at effRate too; `smoothRates` band asymmetric (fast bound on voicedRate keeps pinned anchors on their beats, slow bound on effRate). 60s fit / line-break / API shape unchanged. 92 Jest + standard green. Untouched: client, eval pipeline, transcribe, DB schema. |
| T-1151 | Create GitHub remote + push | done | Remote `https://github.com/eliswedtlv/Pitchmi_new.git`; pushed `main` 2026-07-16. |
| T-1151b | Auth: verify Supabase ES256 JWTs via JWKS | done | Prod project issues ES256 (kid) user JWTs; middleware now verifies against `${SUPABASE_URL}/auth/v1/.well-known/jwks.json` via `jose` (cached), HS256+secret fallback kept. `jose` pinned to 5.x (v6 ESM-only). Committed as T-1151. |
| T-1167 | Tester round 1: pacing fallback, centered prompter, save feedback | done | (A) Pacing (`lib/path.js`): compute anchor coverage; <0.6 → ignore anchors, lay uniformly at measured median rate with 0.35s sentence pauses (`layUniform`); ≥0.6 → keep anchors (`layAnchored`, spare gap becomes pause not slow words) then `smoothRates` caps every word to ±35% of measured rate. 60s fit / line-break / API shape unchanged. (B) Prompter: portrait phones vertically centered (`portrait:top-1/2 -translate-y-1/2`) + soft center-band scrim; landscape keeps top-third. (C) Results: success → disabled "Saved ✓" + "Saved to your videos" toast (EN/HE) with inline "My videos" → `/videos`; error toast on failure; idempotent duplicate-save guard. Server 88 Jest + standard green; client 74 vitest + build + layout 2/2 green. Untouched: eval pipeline, provider routing, transcribe/clean-verbatim, auth, DB schema. |
| T-1166 | Eval: 2fps minimal video proxy before upstream call | done | P0: `/api/evaluate` timed out `eval_upstream_timeout` after 304s on real takes — ~60s portrait mp4 (tens of MB) exceeds the ~20MB base64 ceiling of the Gemini/Vertex backend, stalling every attempt to its 60s abort (75KB probe evaluated in 6.7s). Server-only fix: (1) new `transcodeForEval` (ffmpeg-static) → minimal eval proxy: 2fps, 360px short side (aspect/rotation kept), H.264 crf 32 veryfast, mono AAC 64k, faststart mp4 (~1–3MB); `/api/evaluate` sends the proxy (`video/mp4`) to `evaluateVideo`, original take untouched for Scribe/save. (2) proxy >18MB → `413 take_too_large` (EN/HE message). (3) event timings add `transcode_ms`/`video_bytes_in`/`video_bytes_eval`. (4) tightened T-1165 deadline — `fetchWithTimeout` reads the body under the same per-attempt abort timer so a stalled stream can't run past 240s. 83 Jest + standard green. Untouched: provider pin, prompts, scoring, client flow, DB schema. |
| T-1165 | Server: upstream timeouts + eval deadline + guaranteed event logging | done | P0: `/api/evaluate` hung 299.9s (Railway 499, no event row) — Vertex-pinned OpenRouter fetch had no timeout and retries multiplied the stall. Server-only fix: (1) `fetchWithTimeout` (AbortController, cleared timer) on OpenRouter + Gemini at 60s/attempt, Scribe 60s abort; timeout = retryable 5xx. (2) 240s pipeline deadline threaded into `evaluateVideo({deadline})`, checked before every attempt → throws `code:'eval_upstream_timeout'` (504) when spent. (3) handler logs one event row on EVERY outcome (evaluate/error+timings), responds 504 `eval_upstream_timeout` / 500 `server_error` without `next()`. Client maps 504 → EN/HE "The AI took too long — try again". 81 Jest + standard; 70 vitest + build green. Untouched: provider pin, prompts, scoring, client transport, DB schema. |
| T-1164 | Propagate detected language to client; content-based dir fallback | done | P0: Hebrew still rendered reversed after T-1163 because the client never persisted `result.language` — `project.language` stayed null → dir="ltr" → Prompter isolated every Hebrew word → reversed. Client-only fix: (1) `setProject({...project, language})` after transcribe in `recorder/page.tsx` + home upload; (2) new `lib/textDir.ts` `resolveDir` (language first, sniff script text when missing) used by editor + karaoke; (3) Prompter flips a line's dir when a majority of tokens oppose the passed dir (no per-word `<bdi>`). 69 vitest + layout proof + build green. |
| T-1163 | RTL prompter rebuilt on continuous text flow + clean-verbatim draft | done | (A) P0 RTL root cause: per-word flex/inline-block boxes + `<bdi>` on every word laid words out in DOM order (bidi only reorders within a continuous inline run). Rebuilt `Prompter.tsx` as one inline text run per line (plain `<span>`s + real spaces, `dir`+`text-align:start`), colour-only highlight, `<bdi>` isolate ONLY on opposite-script tokens. Proven by `npm run test:layout` (Playwright real-browser `getBoundingClientRect`, 2 green). (B) `cleanVerbatim.js`: one text-only OpenRouter call turns raw STT into a clean-verbatim draft (same language, no paraphrase), stored as `script` (sentences joined `\n`); silent fallback to deterministic text; `clean_ms` logged; editor shows one sentence/line + EN/HE caption. Server 76 Jest + standard; client 55 vitest + layout proof + build green. Playwright added as client devDep (deviation). |
| T-1162 | Teleprompter redesign + filler stripping + OpenRouter video routing | done | (A) P0: OpenRouter base64-video 500s fixed by pinning `provider: {only:['google-vertex'], allow_fallbacks:false}` (AI-Studio backend takes YouTube only); real mime in data URL; 5xx retry separate from JSON retry; logs `scores.timings.upstream`. (B) Broadcast line-breaker (`path.js` §6 step-7, ≤32ch/6w, phrase boundaries, no orphans/number-splits) + upper-third fixed-reading-line `<Prompter/>` with EN/HE one-time hint. (C) `fillers.js` single source of truth; transcribe strips disfluencies from `script`, keeps `original_words`. Server 65 Jest + standard; client 54 vitest + build green. |
| T-1161 | Mobile UX overhaul + evaluate-transport bug | done | (A) P0: iOS Safari never sent `POST /api/evaluate` — T-1160's XHR path failed after CORS preflight with no handler firing; reverted to `fetch`+`AbortController`, state-driven wait labels (`evalStages`), errors/timeouts route to error screen. (B) Portrait capture (`recorderConstraints`) + correct-aspect playback. (C) Overlay karaoke (`<Prompter/>`) on full-screen preview. (D) RTL: `dir` + `<bdi>` word isolation. Client 49 vitest + build green. |
| T-1160 | QA fixes: eval wait UX + safe-area + demo ad | done | Live iPhone QA. (A) XHR eval with 5-min timeout + upload progress + staged wait screen; Skip Ad never aborts (tested); wake lock re-acquires on visibility; server logs `scores.timings`. (B) Branded 15s demo ad via `genAd`. (C) `viewport-fit=cover` + safe-area padding on bottom CTAs. |
| T-1159 | Eval: force JSON mode + robust parsing | done | Prod P1 "no JSON object in model output" on `/api/evaluate`. JSON mode (OpenRouter `response_format`, Gemini `responseMimeType`); fence-stripping + balanced-block parser; 3rd stricter retry; metadata-only diagnostic event on final failure. `tests/evaluate.test.js` added. |
| T-1153 | Provision Supabase project + run migrations | todo | Create project, run `server/db/migrations/*.sql`, enable anon sign-in, create private `videos` bucket, set env vars, run `scripts/smoke.js`. Eli (manual). |
| T-1154 | Deploy two Railway services | todo | Root `client/` and root `server/`; set env from `.env.example` files. Eli (manual). |
| T-1152 | Swap ad stub for a real ad network | todo | `<AdSlot/>` + `GET /api/ad` contract designed to be swappable. Network TBD (v2 §2.6). |
| T-1155 | Verify OpenRouter video content-part shape | done | Resolved by T-1162: `video_url` snake_case data-URL part confirmed against live docs; base64 video requires the `google-vertex` backend, now pinned via provider routing. Real-key smoke still bundled in `scripts/smoke.js`. |
| T-1156 | Wire admin "surge trip < 24h" banner + service-state read | todo | Derive from `surge_trip` events; optional GET service-state endpoint. |
| T-1200 | PitchMi v2 milestone | future | Google sign-in, public feed, leaderboards, moderation. See `docs/pitchmi-v2-spec.md`; convert to a `/goal` spec after v1 ships and its real code can be inspected. |
