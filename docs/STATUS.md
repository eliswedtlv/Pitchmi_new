# PitchMi — STATUS

## Current state
v1 core loop is built end-to-end (T-1150). **Server** (Express) exposes the full API — projects, transcribe (ElevenLabs Scribe via ffmpeg-static audio extraction), karaoke path, evaluate (timing/accuracy scoring + Gemini delivery eval via OpenRouter/Gemini), save, takes, ad stub, and admin — behind a fail-open kill switch, surge auto-trip, upload cap, JWT auth (Supabase JWKS ES256 with HS256 fallback), and RLS. **Client** (Next.js 15) implements all 8 screens with anonymous Supabase auth, camera recorder, live fit meter, `performance.now()`-based karaoke teleprompter, results, my-videos, and admin. All automated acceptance checks (§13 #1–#13) pass. Not yet run: real external-API smoke (needs keys) and the manual browser checklist below.

## Recently shipped
- 2026-07-22 (T-1166): Eval — 2fps minimal video proxy before the upstream call. **Bug (live 11:59 UTC):** `POST /api/evaluate` failed `eval_upstream_timeout` after 304s (`scribe_ms=2646`, the rest burned in timed-out upstream attempts) while a 75KB probe evaluated in 6.7s. **Root cause:** real takes (~60s portrait 720×1280 mp4, tens of MB) exceed the ~20MB inline/base64 request ceiling of the Gemini/Vertex backend — every oversized attempt stalls until its 60s abort. **Fix (server only):** (1) new `transcodeForEval` in `lib/audio.js` transcodes the take with ffmpeg-static to a minimal eval proxy — 2fps, 360px short side (aspect + rotation preserved), H.264 crf 32 preset veryfast, mono AAC 64k (voice quality kept for scoring), faststart mp4 (~1–3MB for a 60s take) — and `/api/evaluate` sends THIS proxy (`video/mp4`) to `evaluateVideo`; the original take is untouched for Scribe/accuracy and `/api/save`. (2) Hard guard: a proxy over 18MB returns `413 {"error":"take_too_large"}` with an EN/HE `message` (should never fire at these settings). (3) Evaluate event timings now carry `transcode_ms`, `video_bytes_in`, `video_bytes_eval` (numeric metadata only). (4) Tightened the T-1165 deadline: `fetchWithTimeout` now reads the response **body** under the same per-attempt abort timer (not just headers), so a stalled stream after headers can no longer run unbounded past the 240s ceiling — the source of the 304s overrun. Untouched: provider routing pin, prompts, scoring, client flow, DB schema. Server 83 Jest green (new: `transcodeForEval` invoked with the exact ffmpeg args via mocked spawn; 413 oversized-proxy guard skips the upstream + logs one error row; success event carries the new timings) + StandardJS clean.
- 2026-07-21 (T-1165): Server upstream timeouts + eval deadline + guaranteed event logging. **Bug (live 16:25–16:31 UTC):** `POST /api/evaluate` produced NO response for 299.9s until the client's 5-min timeout fired (Railway 499) and logged NO event row — the handler hung inside the eval pipeline because the Vertex-pinned OpenRouter fetch had no timeout and the JSON/5xx retries multiplied the stall. **Fix (server only):** (1) hard per-attempt timeout on every upstream HTTP call — a shared `fetchWithTimeout` (AbortController + always-cleared timer) wraps OpenRouter + Gemini fetches at `PER_ATTEMPT_MS=60s`, and Scribe gets its own 60s abort; a timed-out attempt is treated as a retryable 5xx. (2) Total pipeline deadline of 240s threaded into `evaluateVideo({deadline})`; before every retry/attempt it checks the remaining budget and, when spent, throws a `code:'eval_upstream_timeout'` (status 504) error instead of looping. (3) The `/api/evaluate` handler now guarantees an event row on EVERY outcome — success logs `evaluate`; the catch logs a single `error` row with stage timings (`scribe_ms`/`eval_ms`) so far, then responds 504 `{"error":"eval_upstream_timeout"}` for timeouts or 500 `server_error` otherwise (no `next()`, so no double-log via the global handler). Client wait screen maps 504 → friendly EN/HE "The AI took too long — try again." (`lib/strings.ts` `evalTooLong`), never raw JSON. Untouched: provider routing pin, prompts, scoring, client transport, DB schema. Server 81 Jest green (new: evaluateVideo spent-budget short-circuit + fake-timer hung-upstream → retried → 504 within budget; route 504/500/success each log exactly one row) + StandardJS clean; client 70 vitest green (wait 504 friendly message) + `next build` exit 0.
- 2026-07-21 (T-1164): Propagate detected language to client + content-based dir fallback. **Bug:** despite the T-1163 renderer fix Hebrew still rendered reversed — the client never persisted the detected language. `/api/transcribe` returns `language:"he"` but `recorder/page.tsx` (and the home-upload path) only consumed `result.text`; the store's `project.language` stayed `null` from creation → editor/karaoke computed `dir="ltr"` → the Prompter bidi-isolated *every* Hebrew word (all "oppose" LTR) → words rendered in DOM order LTR = reversed. **Fix (client only):** (1) after `transcribeVideo` succeeds in `recorder/page.tsx` and `page.tsx` upload, `setProject({ ...project, language: result.language })`. (2) New `lib/textDir.ts` (`dirForLang`/`dirForText`/`resolveDir`) — language is the source of truth, but when it's missing, direction is sniffed from the script text (any strong RTL char in the first 100 chars → rtl); editor + karaoke both call `resolveDir`. (3) Prompter hardening: per line, if a majority of tokens oppose the passed `dir`, flip that line's effective `dir` (one clean run) instead of bidi-wrapping every word. Client 69 vitest green (new: recorder persists language after transcribe, textDir Hebrew/English/mixed, Prompter majority-flip → dir=rtl + no per-word `<bdi>`) + layout proof 2/2 + `next build` exit 0. Server untouched. (Also: scoped vitest `include` to `src/**` so it no longer collects the Playwright `tests-layout/` spec.)
- 2026-07-21 (T-1163): RTL prompter rebuilt on continuous text flow + clean-verbatim transcript draft. **(A) P0 RTL — root cause:** T-1161/62 rendered each word as its OWN layout box (line = `flex flex-wrap`, each word a `<bdi className="inline-block">`). The Unicode bidi algorithm only reorders characters *within a continuous inline text run*; separate flex/inline-block boxes are positioned in DOM (source) order regardless of `dir`, so Hebrew words marched left-to-right, and wrapping **every** word in `<bdi>` isolated each word from its neighbours (breaking punctuation too). **Fix (`Prompter.tsx`):** each line is now ONE continuous inline run — a `<p dir=… style="text-align:start">` holding plain inline `<span>`s separated by real space text nodes (no flex/grid/inline-block/float/absolute on words). Highlighting is colour-only (`text-green-400`/opacity), so it can never change a word's box. Type size is a LINE-level prop, not per-word. Only tokens whose script *opposes* the line (Latin brand names inside Hebrew, via a strong-char range check) get a `<bdi>` isolate — nothing else. **Proof:** new `npm run test:layout` (Playwright, real headless Chromium, `client/tests-layout/`) mounts the real Prompter with the Hebrew fixture and asserts via `getBoundingClientRect` that Hebrew word x-centres run strictly right-to-left, the Latin token sits between its Hebrew neighbours in logical order, and an English fixture runs left-to-right — 2 green. **(B) Clean-verbatim draft:** new `server/src/lib/cleanVerbatim.js` adds one cheap text-only OpenRouter call (`EVAL_MODEL`, JSON mode, temp 0.2) after the deterministic filler strip and before storing `script`: removes false starts/stutters/repetitions/self-corrections, fixes obvious STT misrecognitions, adds punctuation/capitalization, same language, never paraphrases/reorders/translates; returns `{sentences:[…]}`. `script` is the sentences joined with `\n`; `original_words` untouched (path algo §6 unchanged — anchors the cleaned script as user edits). Any failure degrades silently to the deterministic text (transcribe still 200); `clean_ms` logged into the transcribe event. Editor shows the draft one sentence per line, 18px/1.6, `dir`+right-align for RTL, with an EN/HE cleanup caption. Server 76 Jest green (cleanVerbatim shape + HTTP-fail/throw fallback, transcribe route success+fallback, path-anchor on cleaned-vs-raw Hebrew) + StandardJS clean; client 55 vitest green (continuous-run spans, isolation of opposite tokens only, text-align:start) + layout proof + `next build` exit 0.
- 2026-07-19 (T-1162): Teleprompter redesign + filler stripping + OpenRouter video routing fix. **(A) P0 eval 500s:** production `/api/evaluate` intermittently 500'd because base64 data-URL video is only accepted by OpenRouter's **Vertex** Gemini backend, while the load-balanced **AI-Studio** backend takes YouTube URLs only. `callOpenRouter` now pins `provider: { only: ['google-vertex'], allow_fallbacks: false }`, keeps the REST `video_url` snake_case content part (verified against live OpenRouter docs), passes the take's **real** mime into the data URL, retries transient upstream 5xx up to 2× with short backoff (separate from the 200-bad-body JSON retries, which no longer fire on transport errors), and logs the served upstream into `events.scores.timings.upstream` (metadata only). No Gemini-direct fallback added; `EVAL_PROVIDER` unchanged. **(B) Teleprompter:** server line-breaker rebuilt (`path.js` §6 step-7, timing algo untouched) to broadcast standard — ≤32 chars / ≤6 words per line, breaks at sentence→comma→conjunction→preposition boundaries, never a 1-word orphan (rebalanced) and never a number split from its unit; lines expose start/end times. Client `<Prompter/>` moved to the **upper third** (near the front camera), renders a fixed 3-line window (previous 50% / active large `clamp(26–44px)` green-highlighted word / next 70%) with the reading line pinned while the column smooth-scrolls under it; one-time localized hint (EN/HE `lib/strings.ts`) dismissed on first countdown; `<bdi>` + dir-aware alignment kept. **(C) Fillers:** new `server/src/lib/fillers.js` is the single source of truth (shared by transcribe + `score.js`); `/api/transcribe` strips disfluencies (EN + HE list, prefers a provider disfluency tag when present) from the editable `script` while keeping the full word list with timestamps as `original_words` for the path algorithm. Server 65 Jest green (new `fillers`, `lineChunker`, OpenRouter request-shape + 5xx-retry tests) + StandardJS clean; client 54 vitest green (Prompter 3-line window / fixed active line / RTL align / hint-once) + `next build` exit 0.
- 2026-07-19 (T-1161): Mobile UX overhaul + evaluate-transport P0 fix. **(A)** `evaluateVideo` reverted from XHR back to the proven `fetch` transport (identical to `transcribeVideo`) with an `AbortController` enforcing `EVAL_TIMEOUT_MS` — fixes the iOS-Safari bug where the POST never left the browser (see Known issues). Wait screen is now a state-driven 2-stage machine (`lib/evalStages.ts`: Uploading→Analyzing, never a clock-driven "Scoring" lie); any failure/timeout routes to the error screen instead of an eternal spinner. **(B) Portrait capture:** `lib/recorderConstraints.ts` requests portrait 720×1280 on portrait viewports, landscape 1280×720 on desktop; recorder/karaoke previews fill full-screen (`object-cover`); results + my-videos players use `object-contain`/`max-h` so portrait and landscape both play at correct aspect. **(C) Overlay karaoke:** new `<Prompter/>` renders subtitles OVER the full-screen preview (bottom-third scrim, ~3 lines, active word highlighted, countdown overlaid); take-1 recorder countdown gets the same overlay treatment. **(D) RTL:** prompter container + lines carry `dir`, every word wrapped in `<bdi>`; results coach comments `dir="auto"`; editor textarea keeps explicit `dir` from detected language. New client tests: `evaluate` transport (success/network-error/timeout), `evalStages` machine, `recorderConstraints`, `Prompter` RTL, plus wait-page error routing. Client 49 vitest green + `next build` exit 0; server 48 Jest green + StandardJS clean (server untouched).
- 2026-07-19 (T-1160): Production QA from live iPhone testing. **(A) Eval wait:** `evaluateVideo` now uses XMLHttpRequest with an explicit 5-min timeout (`EVAL_TIMEOUT_MS`) + real upload-progress, replacing timeout-less `fetch` (fixed Railway 499 "client closed" at 71s/112s). Wait screen shows honest staged progress (Uploading %→Transcribing→AI watching→Scoring; stage 0 advances on actual upload completion, rest on a timer). "Skip Ad" only hides the ad element — never aborts/navigates the pending request (new `wait.test.tsx` asserts this with fake timers). `useWakeLock` re-acquires on `visibilitychange` so the lock survives screen dims for the whole wait. Server: evaluate event now carries `scores.timings {scribe_ms, eval_ms, attempts}` (metadata only). **(B) Demo ad:** replaced the ffmpeg color-bar clip with a 15s branded placeholder (dark diagonal gradient, "PitchMi" wordmark, "Nail your minute." tagline, subtle sin-period breathing → seamless loop) via `make-sample.js` `genAd`; `GET /api/ad` contract unchanged. **(C) iOS safe-area:** `viewport-fit=cover` (layout `viewport` export) + `.safe-b-*`/`.safe-pos-4` env(safe-area-inset-bottom) utilities applied to bottom CTAs on home/editor/results/admin/videos/karaoke. Server 48 Jest green + StandardJS clean; client 30 vitest green + `next build` exit 0.
- 2026-07-19 (T-1159): Eval — fixed prod P1 "no JSON object in model output" on `/api/evaluate`. Forces JSON mode (OpenRouter `response_format: json_object`, Gemini `responseMimeType: application/json`, both at temp 0.2). Hardened parsing: strip markdown fences, extract first balanced `{...}` block tolerating prose. Added a 3rd stricter retry (initial + retry + reminder "Return ONLY the JSON object"). On final failure logs a metadata-only `error` event (`provider`, output length, fenced-flag — never the model text, per privacy rule). New `tests/evaluate.test.js` (7 cases: plain/fenced/prose/nested/garbage + retry-count + diagnostic); 48 Jest tests green, StandardJS clean.
- 2026-07-18 (T-1151): Auth — server middleware now verifies Supabase user JWTs against the project JWKS (ES256, modern asymmetric signing keys) via `jose` `createRemoteJWKSet`+`jwtVerify` (key set cached across requests); HS256+`SUPABASE_JWT_SECRET` retained as fallback for legacy projects and tests. Fixes prod 401s from project `ciknhdocqyuxlzsnhzbc`. New `tests/auth.test.js` (6 cases: ES256 valid/wrong-key/expired, HS256 pass/wrong-secret, no-header); 41 Jest tests green, StandardJS clean.
- 2026-07-16 (T-1150): Client — Next.js 15 App Router (8 screens), useRecorder/useKaraokeClock/useWakeLock hooks, `<AdSlot/>`, Supabase anon auth, API client, Zustand store, Tailwind v4 + shadcn-style UI. `next build` exit 0; 29 vitest tests green (fit meter + karaoke clock w/ fake timers). Fixed admin logs/aggregates response-shape + field-name mismatches.
- 2026-07-16 (T-1150): Server API — all routes + middleware (kill switch, surge, upload cap, auth, admin cookie), Scribe/ffmpeg/eval integrations, DB migrations + RLS, eval prompt, smoke script, sample/demo video fixtures. 35 Jest tests green; StandardJS clean.
- 2026-07-16 (T-1150): Server CORE IP — karaoke path (§6) + take scoring (§7), 21 unit tests over all edge cases.
- 2026-07-16 (T-1150): Repo scaffolding — git init (`main`), `.gitignore`, root `CLAUDE.md`, `README.md`, docs.

## In progress
- Nothing in progress. v1 core loop code-complete pending real-key smoke + manual QA.

## Known issues / deviations
- **Root cause + fix (T-1161, P0 evaluate transport):** *Current behavior* — on iPhone iOS Safari the evaluate `OPTIONS` preflight returned 204 but the `POST /api/evaluate` never reached the server (nothing logged), while the wait screen advanced clock-driven labels to "Scoring your delivery" and hung forever. The same take transcribed fine seconds earlier. *Reproduction* — desktop Chrome could not reproduce (fetch and XHR both work there); the failure is iOS-Safari-specific and was observed on-device. *Root cause* — the only thing that changed between the working transcribe path (fetch) and the broken evaluate path was T-1160's rewrite of `evaluateVideo` to `XMLHttpRequest` with `xhr.timeout = 5min` + `xhr.upload` progress on a cross-origin request. After the CORS preflight succeeded WebKit never dispatched the actual POST body, and crucially **none** of the XHR handlers (`onload/onerror/ontimeout/onabort`) fired — so the promise never settled and the timer-driven labels kept lying about progress. *Chosen fix* — revert `evaluateVideo` to the identical `fetch` transport `transcribeVideo` uses, with an `AbortController` enforcing `EVAL_TIMEOUT_MS`; drop byte-level upload % (reliability > nice-to-have); replace the timer-driven labels with a state-driven 2-stage machine (`lib/evalStages.ts`) so any resolve→results / reject→error-screen outcome is honest and no post-response label ("Scoring") can appear before a server response. See `client/src/lib/__tests__/evaluate.test.ts` (success/network-error/timeout) and `evalStages.test.ts`.
- **Deviation (T-1160):** evaluate stage timings ride inside the existing `events.scores` jsonb under a `timings` key (not new columns) to honor "do not touch DB schema". Aggregates only read `scores.overall`, so they're unaffected; values are numeric metadata only.
- **Note (T-1161):** recorder + karaoke are now full-screen previews (`fixed inset-0`); their bottom-anchored Stop buttons use `.safe-pos-4` to clear the iOS home indicator (supersedes the T-1160 note that the recorder had no anchored CTA).
- **Deviation:** `jose` pinned to 5.x (not 6.x). v6 is ESM-only; the server is CommonJS and the Jest runtime can't `requireActual` an ESM-only module. v5 ships dual CJS/ESM. `jose` was not a pre-pinned library.
- **Deviation:** `multer` pinned to 2.x (not 1.x) — 1.x has published CVEs; multer was not a pinned library. API compatible.
- **Deviation:** added `original_words jsonb` column to `projects` (beyond §4). `/api/path` needs take-1 word timings and §4's schema had nowhere to persist them. Reflected in `server/db/migrations/0001_init.sql`.
- **Deviation:** local Node is v24.11.1; spec pins Node 22 (Railway runtime). v24 is backward-compatible for dev; `engines` allows `>=22`.
- **Deviation:** client `next@15.1.0` (nearest 15.x); all other versions as pinned.
- **Resolved (T-1162):** OpenRouter base64-video 500s are fixed by pinning provider routing to `google-vertex` with `allow_fallbacks:false` (AI-Studio, the failing backend, accepts YouTube URLs only). The `video_url` snake_case data-URL content part is confirmed correct against live OpenRouter docs; real per-take mime is used. The direct-Gemini `inline_data` fallback (`EVAL_PROVIDER=gemini`) remains available but is no longer the reliable path.
- **Deviation (T-1163):** added `@playwright/test` (resolved 1.61.x) as a client devDependency for the RTL layout proof (`npm run test:layout`); chromium installed via `npx playwright install chromium`. Playwright was not a pre-pinned library — it runs a real browser, which jsdom/vitest cannot, and is the acceptance gate the spec required. A dev-only route `client/src/app/dev/prompter-layout/[fixture]/page.tsx` renders the real Prompter for the test; it is not linked from the product UI.
- **Gap:** admin Service tab toggle is optimistic (no GET service-state endpoint in the fixed API list) and the "surge trip < 24h" alert banner is not yet wired. Minor; logs already record `surge_trip` events.
- External-API integration is NOT run in CI. `server/scripts/smoke.js` runs the full pipeline against the bundled 3s sample when keys are present; reports "NOT RUN" otherwise.

## Next
- Provision Supabase (run `server/db/migrations/0001_init.sql`, enable anonymous sign-in, create private `videos` bucket), set env, run `node --env-file=.env scripts/smoke.js` to validate Scribe + eval with real keys.
- Deploy two Railway services (root `client/`, root `server/`).
- Manual QA (below), then swap the ad stub for a real network (T-1152) as v2 approaches.

## Manual test checklist (for Eli)
- [ ] (T-1166) A real ~60s portrait take now evaluates end-to-end in seconds (no more 304s timeout): confirm the results screen appears; admin Logs shows an `evaluate` row whose `scores.timings` has `video_bytes_eval` at ~1–3MB (well under `video_bytes_in`) and a small `eval_ms`.
- [ ] (T-1165) A slow/hung evaluation no longer hangs the app: within ~4 min the wait screen shows "The AI took too long — try again" (HE localized when the take is Hebrew), not an eternal spinner or raw JSON; admin Logs shows an `error` event row (`eval_upstream_timeout`) with stage timings for that attempt.
- [ ] (T-1163) Hebrew karaoke reads correctly on iPhone: words run right-to-left, the Latin brand token ("PitchMi") sits in the right place mid-sentence, punctuation attaches correctly, per-word green highlight still lands on time.
- [ ] (T-1163) Hebrew transcript draft reads naturally sentence-by-sentence in the editor (one sentence per line, right-aligned) — the speaker's own words, just cleaned (no stutters/repeats), not paraphrased or translated.
- [ ] (T-1162) iPhone Hebrew karaoke readability: prompter sits in the **upper third** near the front camera, active line is large + easy to follow, 3-line window scrolls smoothly with the reading position fixed, per-word green highlight lands on time; one-time hint shows on first use only.
- [ ] (T-1162) `/api/evaluate` completes twice in a row on device (no intermittent 500) — confirm `events.scores.timings.upstream` reads `google-vertex`.
- [ ] iPhone Hebrew karaoke take end-to-end: overlay subtitles sit ON the video (bottom third), RTL word order + per-word highlight correct, countdown overlaid, take-1 and karaoke both portrait.
- [ ] Portrait output plays correctly (upright, not sideways/letterboxed) in the results player AND after Download / Save-to-cloud playback in My Videos.
- [ ] iOS Safari: evaluate actually completes (POST reaches server) after the XHR→fetch fix; on a forced network drop the error screen appears — never an eternal spinner.
- [ ] iOS Safari: bottom CTAs (Rehearse next take, Save to cloud, Record a take, Stop) clear of the Safari toolbar / home indicator.
- [ ] Long take (~60s): eval wait survives 2+ min without the client giving up; staged progress advances; screen stays awake; "Skip Ad" hides the ad but keeps waiting.
- [ ] New demo ad plays and loops seamlessly on the wait screen (with the "Ad" label + Skip button).
- [ ] Camera record on Chrome desktop + iOS Safari (webm vs mp4 fallback).
- [ ] Karaoke highlight sync feels right (word lands on time).
- [ ] Hebrew take end-to-end incl. RTL prompter + editor.
- [ ] Share sheet on mobile (Web Share API with file); hidden when unsupported.
- [ ] Download vs Save-to-cloud are distinct; nothing is stored unless Save is tapped.
- [ ] Admin login → Logs / Aggregates / Service tabs render against real data.
